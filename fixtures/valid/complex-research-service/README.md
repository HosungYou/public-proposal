# Complex research-service surface fixture

This sanitized fixture exercises the reusable surface contract with three
Word-native table roles (evaluation crosswalk, evidence ledger, and RACI) and
four deterministic SVG families (framework, Gantt, RACI, and decision flow).

It is a contract fixture, not a proposal source. It contains no issuer,
personal, pricing, or performance claims. The fixture is valid only when the
DOCX, SVG bytes, page images, and render-manifest hashes agree.
