{
  "name": "kpp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22 <27"
  },
  "scripts": {
    "build": "npm run build --workspace @longtable/kpp-schemas && npm run build --workspace @longtable/kpp-core && npm run build --workspace @longtable/kpp-renderers && npm run build --workspace @longtable/kpp-audits && npm run build --workspace @longtable/kpp-cli && npm run build --workspace @longtable/public-proposal",
    "pack": "npm pack --workspace @longtable/kpp-schemas --dry-run && npm pack --workspace @longtable/kpp-core --dry-run && npm pack --workspace @longtable/kpp-renderers --dry-run && npm pack --workspace @longtable/kpp-audits --dry-run && npm pack --workspace @longtable/kpp-cli --dry-run && npm pack --workspace @longtable/public-proposal --dry-run",
    "test": "vitest run --no-file-parallelism",
    "typecheck": "tsc --noEmit -p tsconfig.base.json && npm run typecheck --workspace @longtable/public-proposal",
    "verify:public-proposal": "node scripts/verify_public_proposal_release.mjs"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "tsx": "4.23.12",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
